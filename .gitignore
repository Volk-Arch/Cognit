# Модели — слишком большие для git (гигабайты)
models/

# Cognit — паттерны не версионируются (бинарные, привязаны к модели)
echo_patterns/

# agents/ хранятся в клиентском проекте, не здесь
agents/

# Python
__pycache__/
*.pyc
*.pyo

# Конфиг с абсолютными путями — у каждого свой
.echo.json

# IDE
.claude/
.idea/
.venv/
# Cognit — паттерны не версионируются, хранятся локально или в S3
